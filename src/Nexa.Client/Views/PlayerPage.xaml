<Page
    x:Class="Nexa.Client.Views.PlayerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nexa.Client.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar DefaultLabelPosition="Right">
            <AppBarButton Icon="Back" Label="Back" Click="BackButton_Click"/>
            <AppBarElementContainer>
                <TextBlock x:Name="TitleTextBlock" Text="Player" VerticalAlignment="Center" Margin="12,0,0,0" Style="{StaticResource SubtitleTextBlockStyle}"/>
            </AppBarElementContainer>
        </CommandBar>

        <WebView2 x:Name="PlayerWebView" Grid.Row="1"/>
        
        <ProgressRing x:Name="LoadingRing" Grid.Row="1" IsActive="True" Width="60" Height="60" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Page>
